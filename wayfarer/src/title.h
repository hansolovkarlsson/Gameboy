// The "WAYFARER" / "PRESS START" screen the player sees before
// gameplay begins - main.c calls title_screen() once, blocking until
// Start is pressed. See title.c for the tile/palette data.

#ifndef WAYFARER_TITLE_H
#define WAYFARER_TITLE_H

void title_screen(void);

#endif
