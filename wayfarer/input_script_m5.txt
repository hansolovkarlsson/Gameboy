# Wayfarer Milestone 5 verification script (room.c door/key.c/world.c
# lock-gating). Starts centered in room (0,0) (72,64) - now walled on
# every side except east (its south side was permanently severed this
# milestone, breaking the old 4-room cycle into a dead-end chain).
#
# 1. Hold RIGHT: crosses (0,0)'s open east edge into room (1,0), where
#    the one key sits at that room's own center - continuing to hold
#    RIGHT walks straight through it, collecting it (confirmed
#    separately via a throwaway probe that a locked door blocks
#    movement and shows a distinct door texture before this point -
#    see docs/GAMEBOY_ROADMAP.md).
# 2. Hold DOWN: crosses (1,0)'s open south edge into room (1,1), where
#    the one heart pickup sits (Milestone 4) - collected incidentally
#    along the way isn't required for this script's own point, just
#    visible in the final frame.
# 3. Hold LEFT: with the key already collected, (1,1)'s west side is no
#    longer a locked door - walking into it now transitions into room
#    (0,1), a room otherwise unreachable (its own north side is
#    severed to match (0,0)'s, and its west/south sides are the grid's
#    own edges) - reachable *only* through the now-unlocked door.
#
# Every frame number re-derived empirically (capture/inspect candidate
# frames), not guessed - held longer than a naive distance calculation
# would suggest, since the first attempt at each leg undershot before
# being extended. Confirmed stable (byte-identical) from frame 330
# onward.
5 RIGHT down
150 RIGHT up
155 DOWN down
230 DOWN up
235 LEFT down
320 LEFT up
