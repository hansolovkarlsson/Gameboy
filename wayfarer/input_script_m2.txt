# Wayfarer Milestone 2 verification script (room.c/world.c). Starts
# centered in room (0,0) (72,64) and walks a path that exercises both
# axes of transition plus a real regression check that a *closed* side
# still blocks movement in a non-origin room:
#
# 1. Hold RIGHT: crosses room (0,0)'s open east edge, cutting into
#    room (1,0) (fingerprint: west+south open, north+east walled).
# 2. Hold UP: room (1,0)'s north side is walled (room_y=0, same as the
#    start room) - confirms collision still stops movement cleanly at
#    ROOM_MIN_Y in a room that isn't the origin room.
# 3. Hold DOWN: crosses room (1,0)'s open south edge, cutting into room
#    (1,1) (fingerprint: west+north open, south+east walled) - a
#    second, chained transition.
#
# Every frame number re-derived empirically (capture/inspect candidate
# frames), not guessed. Confirmed stable (byte-identical) from frame
# 345 onward.
10 RIGHT down
95 RIGHT up
100 UP down
180 UP up
185 DOWN down
340 DOWN up
