# Prism Milestone 6b verification script. Same board interaction as
# input_script_m5.txt (column 0, rows 2/3 hold Y/G on this build's
# deterministic board; select (0,3), swap up with (0,2): creates a
# vertical Y,Y,Y match at column 0 rows 3-5) - but now gated behind an
# explicit Start press to dismiss the title screen first (title.c), and
# every subsequent event's frame number shifted +20 to give generous
# margin past the confirmed-stable game-grid frame (empirically stable
# from frame 15 onward after the Start press at frame 10/11 - see
# docs/GAMEBOY_ROADMAP.md's Phase 10 Milestone 6b entry). initrand(DIV_REG)
# stays the first line of main(), before title_screen()'s own wait, so
# this is still the exact same deterministic board input_script_m5.txt
# used - only the frame numbers moved, not the board.
10 START down
11 START up
30 DOWN down
31 DOWN up
40 DOWN down
41 DOWN up
50 DOWN down
51 DOWN up
60 A down
61 A up
70 UP down
71 UP up
80 A down
81 A up
