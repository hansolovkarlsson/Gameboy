# Prism Milestone 5 verification script. Note this build's initial
# random board differs from Milestone 4's own script's board (the exact
# DIV_REG value initrand() samples shifted once hud.c/board.c's new code
# was added, changing pre-initrand() instruction timing - still fully
# deterministic *for this exact build*, just not the same seed a
# different build produced - see docs/GAMEBOY_ROADMAP.md's Phase 10
# Milestone 5 entry). Column 0, rows 2/3 hold Y/G on this board; select
# (0,3), swap up with (0,2): creates a vertical Y,Y,Y match at column 0
# rows 3-5, which should increase the score and decrement moves.
10 DOWN down
11 DOWN up
20 DOWN down
21 DOWN up
30 DOWN down
31 DOWN up
40 A down
41 A up
50 UP down
51 UP up
60 A down
61 A up
